{
  "name": "messaging-api",
  "version": "1.0.0",
  "description": "messaging api",
  "main": "app.js",
  "scripts": {
    "osx-dev": "export NODE_ENV=dev && nodemon app.js",
    "osx-test": "export NODE_ENV=test && mocha --require co-mocha test/**.spec.js",
    "osx-prod": "export NODE_ENV=prod && node app.js",
    "win-dev": "set NODE_ENV=dev && nodemon app.js",
    "win-test": "set NODE_ENV=test && mocha --require co-mocha test/**.spec.js",
    "win-prod": "set NODE_ENV=prod && node app.js"
  },
  "author": "Liam Vovk",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.0",
    "body-parser": "^1.15.2",
    "dotenv": "^4.0.0",
    "express": "^4.14.0",
    "jsonwebtoken": "^7.4.1",
    "mongoose": "^4.9.7"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^6.0.0",
    "chai-http": "^3.0.0",
    "co-mocha": "^1.2.0",
    "mocha": "^3.3.0",
    "mongodb": "^2.2.28",
    "nodemon": "^1.11.0"
  }
}
